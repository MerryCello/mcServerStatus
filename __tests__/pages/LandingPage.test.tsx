/** @jest-environment jsdom */
describe('LandingPage', () => {
  test.todo('should call onUpClick when the UP button is clicked on desktop');
  test.todo(
    'should call onDownClick when the DOWN button is clicked on desktop',
  );
  test.todo(
    'should call onUpClick when the SHIFT+UP button is pressed on desktop',
  );
  test.todo(
    'should call onDownClick when the SHIFT+DOWN button is pressed on desktop',
  );
  test.todo(
    'should call onUpClick when the SHIFT+UP button is pressed on mobile',
  );
  test.todo(
    'should call onDownClick when the SHIFT+DOWN button is pressed on mobile',
  );
});
